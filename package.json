{
  "name": "swtool",
  "version": "1.1.1",
  "description": "Browser-based Spacewalk text-to-binary converter",
  "type": "module",
  "bin": {
    "swtool": "./bin/swtool.mjs"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:cli": "bun build bin/swtool.mjs --compile --outfile dist-cli/swtool"
  },
  "devDependencies": {
    "sass": "^1.97.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "h5wasm": "^0.9.0",
    "jsfive": "^0.4.0",
    "pako": "^2.1.0"
  }
}
